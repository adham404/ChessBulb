<template>
  <div>
      <div>please sign up</div>
      <!-- TODO create anchor tag that routes to [Login] (2min) -->
      <!-- TODO Create Form with 5 input field and assign them to the following data properties (FirstName, LastName, UserEmail, UserPassword, ConfirmPassword) (5min) -->
      <!-- TODO Create 'SignUp' button assigned to the function {SignUp} (2min) -->
  </div>
</template>

<script>
export default {
    //TODO Assign Data Properties in the vue data object which are (FirstName(S), LastName(S), UserEmail(S), UserPassword(S), ConfirmPassword(S)) (1min)
    //TODO Assign Firebase (1min)
    //TODO Define SignUp function (1min)
    //TODO First Check if the (UserPassword) is the same as the (ConfirmPassword) if correct proceed if not check show validation Message (2min)
    //FIXME use firebase auth to sign up user account using the following data properties (FirstName, LastName, UserEmail, UserPassword) (20MIN)
}
</script>

<style>
      /* TODO import Styling script from Documentation and Adjust the Component (5min) */

</style>

//TODO time to finish the Component (39MIN)